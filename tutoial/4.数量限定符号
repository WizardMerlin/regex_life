数量限定符号(appears_time.sh)
主要是限制某些成分的出现次数, 例如
"[0-9][0-9][0-9]"就可以写成"[0-9]{3}" 精确匹配, 出现3次.

总结起来大致如下:
{m}     m Repetitions
{m,n}   m to n Repetitions
*       Zero or more repetitions
+       One or more repetitions
?       Optional character

仔细说明一下, 应该是如下的结果:
"?"表示紧跟在它前面的匹配0~1次", 例如:
"[0-9]?\.[0-9]"
可以用来匹配小数, 但是得到的结果却是
10.x3.16.197
10.40.67.173
8.8.8.8
1.2

可以修改一下(借助位置限定):
"^[0-9]?\.[0-9]$"
(必须以digit.digit这单词开头并且结果)

"+" 紧跟着它前面的匹配1次或者多次;
"*" 紧跟着它前面的匹配0次或者多次;

{x}  紧跟着它前面的单元精确匹配x次
{x,} 紧跟着它前面的单元至少匹配x次
{,y} 紧跟着它前面的单元最多匹配y次
{x,y}紧跟着它前面的至少匹配x次, 最多匹配y次

举个例子:
匹配一个[100-1000)的数字
"[1-9][0-9]{2}$"

C语言的标识符匹配:(不能以数字开头的, 字符,数字,_表示的标识)
"[a-zA-Z_]+[a-zA-Z_0-9]*"
(前面的比分必有,所以用"+", 后面的部分可有可无, 所以用"*")


再次注意: grep这个工具是打印出匹配的行, 而不是完全匹配.

